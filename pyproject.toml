
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aparecium-v2"
version = "0.1.0"
description = "Pooled-only embedding inversion for crypto tweets"
authors = [{name="Your Org"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "torch>=2.0",
  "transformers>=4.40",
  "tqdm"
]
